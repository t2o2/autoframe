# Agent credentials — never commit
.auto-claude/.env

# Session logs (large, local-only)
scripts/autonomous-agent-logs/
scripts/autonomous-review-logs/
scripts/autonomous-approve-logs/

# macOS
.DS_Store

# Thought store — ticket artifacts are ephemeral (local-only)
thoughts/tickets/
