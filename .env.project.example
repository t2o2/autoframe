# Project-specific credentials — copy to .env.project and fill in values.
# .env.project is loaded on top of .env; symlink it to switch projects:
#   ln -sf ~/secrets/kaleidoscope.env .env.project

# ── Alpaca ────────────────────────────────────────────────────────────────────
# Sandbox keys from: https://app.alpaca.markets/paper-trading/overview > API Keys
ALPACA_KEY=
ALPACA_SECRET=
ALPACA_ACCOUNT_ID=
