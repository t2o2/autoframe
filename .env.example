# ══════════════════════════════════════════════════════════════════════════════
# To onboard a project you need THREE things. Fill these in and you're set:
#   1. Git repo + token        → GIT_REPO_URL + GITHUB_TOKEN   (clone/push)
#   2. Linear key + team       → LINEAR_API_KEY + LINEAR_TEAM_KEY  (tickets)
#   3. Slack token + channel   → SLACK_BOT_TOKEN + SLACK_CHANNEL  (feedback)
# Everything below those is optional and has sane defaults.
# ══════════════════════════════════════════════════════════════════════════════

# ── 1 of 3 · Linear ──────────────────────────────────────────────────────────
# API key from Linear → Settings → API → Personal API keys
LINEAR_API_KEY=lin_api_your_key_here

# Team key from Linear → Settings → Team → Team key (e.g. ENG, ACME, GYL).
# Every agent files into this team; the spec-loop run tickets land here too.
LINEAR_TEAM_KEY=ENG

# ── 2 of 3 · Slack (human feedback channel) ──────────────────────────────────
# How the agents reach a human for input (ask-human.sh) — questions are posted
# to the channel and the human replies IN THE THREAD with the option number or
# free text. Zero infra: no interactivity Request URL / Socket Mode needed.
#
# Setup (~2 min): api.slack.com/apps → Create App → add Bot Token Scopes
#   chat:write, channels:history  (groups:history if the channel is private)
# → Install to Workspace → copy the Bot User OAuth Token (xoxb-…)
# → invite the bot to the channel:  /invite @your-bot
SLACK_BOT_TOKEN=xoxb-your-bot-token
# Channel id (recommended, e.g. C0123ABCD) or name (e.g. #dev-feedback).
SLACK_CHANNEL=#dev-feedback

# ── Slack ticket intake bot (node main.js slack-listen) ───────────────────────
# Dedicated channel for ticket creation — any root message starts a refinement
# conversation. If unset, SLACK_CHANNEL is used. Same bot token; just invite it
# to this channel too: /invite @your-bot
#
# Required to run slack-listen (the bot REFUSES TO START if any are missing):
#   SLACK_BOT_TOKEN       xoxb-…  — set above; the SAME bot token is reused here
#   SLACK_APP_TOKEN       xapp-…  — below; app-level token for Socket Mode buttons
#   SLACK_TICKET_CHANNEL  or SLACK_CHANNEL (set above)
#   ANTHROPIC_API_KEY     or CLAUDE_CODE_OAUTH_TOKEN (Claude powers the chat)
#   LINEAR_API_KEY        + LINEAR_TEAM_KEY (set above)
#
# Bot token scopes: chat:write, channels:history (groups:history if the channel
#   is private) — granted above — PLUS reactions:write (the bot reacts :eyes: on
#   each user message while processing, then removes it once it has replied).
#SLACK_TICKET_CHANNEL=#new-tickets
#
# The bot presents the draft ticket as Approve/Cancel BUTTONS (not "reply yes"),
# so it needs Socket Mode to receive the click:
#   api.slack.com/apps → your app → Socket Mode → toggle ON
#   → Interactivity & Shortcuts → toggle ON
#   → Basic Information → App-Level Tokens → Generate (scope: connections:write)
#   → copy the App-Level Token (xapp-…)
SLACK_APP_TOKEN=xapp-your-app-token
#
# ask-human.sh option prompts also render as Slack buttons. The slack-listen bot
# is the single Socket Mode consumer and relays each click back to the waiting
# CLI via Redis, so that flow needs slack-listen running + REDIS_URL shared
# (set automatically in docker-compose). Free-text asks / no Redis fall back to
# thread replies.

# Telegram is supported as a fallback if you prefer it over Slack — set both of
# these instead of the SLACK_* pair (tappable inline buttons).
#TELEGRAM_BOT_TOKEN=
#TELEGRAM_CHAT_ID=

# ── Fleet health monitor (agent-monitor service) ──────────────────────────────
# The `monitor` service watches all agent containers from the host and alerts
# the feedback channel (reuses SLACK_*/TELEGRAM_* above) on: full disk,
# crashed/looping containers, hung poll loops, and livelocks (agents claiming
# nothing despite actionable tickets — e.g. a full disk making every ticket
# look "locked"). Run it with:  docker compose --profile monitor up -d
# (it also starts under the `all` / `all-pi` profiles). All knobs are optional.
#MONITOR_CHECK_INTERVAL=120     # seconds between health sweeps
#MONITOR_DISK_WARN_PCT=85       # alert when a container's root fs is >= this %% full
#MONITOR_LOG_WINDOW_MIN=15      # log window for the livelock detector
#MONITOR_ALERT_REMIND_SECS=21600  # re-remind interval for a standing problem (6h)

# ── API credentials ───────────────────────────────────────────────────────────
#
# Option A — Claude.ai subscription (default, no API cost)
#   Leave both keys empty. The container forwards your host OAuth session
#   from ~/.claude/credentials.json — no extra setup required.
#
# Option B — OpenRouter (fallback; billed per token)
#   Get from: https://openrouter.ai/settings/keys
OPENROUTER_API_KEY=
# Models for each tier. Find model IDs at openrouter.ai/models
OR_MODEL_NORMAL=deepseek/deepseek-v4-pro
OR_MODEL_ADVANCED=deepseek/deepseek-v4-pro
#
# Option C — Anthropic direct API key
#   Get from: https://console.anthropic.com/settings/api-keys
ANTHROPIC_API_KEY=

# ── 3 of 3 · Git ─────────────────────────────────────────────────────────────
# Repository the container will clone and work in.
GIT_REPO_URL=https://github.com/your-org/your-repo.git

# Branch to clone from (default: develop).
GIT_BASE_BRANCH=develop

# GitHub personal access token for HTTPS cloning and pushing.
# Needs: repo (read + write), workflow (if pushing CI changes).
# Leave blank if using SSH — mount ~/.ssh instead (see docker-compose.yml).
GITHUB_TOKEN=ghp_your_token_here

# ── Docker platform ───────────────────────────────────────────────────────────
# linux/arm64 for Apple Silicon (M1/M2/M3), linux/amd64 for Intel/AMD
DOCKER_PLATFORM=arm64
DOCKER_TARGETARCH=arm64

# ── Spec-feedback loop (spec-loop / spec-loop-pi profiles) ────────────────────
# Change-driven audit agent: polls GIT_BASE_BRANCH and, when new commits land,
# blind-describes the affected spec-map areas, compares against the docs/ spec
# corpus, and files divergences as Linear Backlog tickets — intraday, minutes
# after a merge. All values optional — defaults shown.
# The ONLY project-specific input is docs/spec-map.yaml in the target repo.
# A repo without one makes this agent idle — so the same autoframe image plugs
# into any project. The runner + agent defs are bundled and seeded automatically.
#SPEC_LOOP_POLL_INTERVAL_S=300  # seconds between branch polls (300 = 5 min)
#SPEC_LOOP_AREAS=               # force these areas on every audited change (empty = audit only changed areas)
#SPEC_LOOP_PUSH_REPORTS=0       # 1 = also bot-commit docs/reviews/** back to GIT_BASE_BRANCH (default: tickets only)
# When pushing, the agent CREATES a fresh per-run Linear tracking ticket and uses
# its id as the commit prefix (lands in Done, not Backlog) — so each writeback
# references a distinct real ticket, never a reused static number.
#SPEC_LOOP_CREATE_RUN_TICKET=1  # 0 = push with empty prefix (only if the repo has no ticket-requiring commit hook)
#SPEC_LOOP_COMMIT_PREFIX=       # explicit override; set only to force a fixed prefix instead of the per-run ticket id
